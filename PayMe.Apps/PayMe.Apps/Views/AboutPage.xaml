<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PayMe.Apps.Views.AboutPage"
             Title="{Binding Title}">
    
    <ScrollView Orientation="Vertical">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <StackLayout x:Name="ContentImage" Grid.Row="0" BackgroundColor="{StaticResource Primary}" 
                     VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
                <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                    <ContentView Padding="20,40,0,40" VerticalOptions="FillAndExpand">
                        <Image Source="corderoski_logo.png" VerticalOptions="Center" HeightRequest="64"/>
                    </ContentView>
                </StackLayout>
            </StackLayout>
            <StackLayout x:Name="ContentDescription" Grid.Row="1" Orientation="Vertical" Padding="15, 10, 15, 0" Spacing="0" 
                     VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            </StackLayout>
        </Grid>
    </ScrollView>
</ContentPage>